# fde-lp — FDE（フォワードデプロイドエンジニア）LP（求職者向け）

- 公開URL: https://fde.agent-best.net/ （GitHub Pages・HTTPS強制）2026-08-29 公開済み
- 構成: `index.html` 1ファイル完結。CSS/JSはインライン、**外部CDNは読み込まない**。`CNAME` あり。
- ロゴとファビコンだけは画像ファイル（同ディレクトリの `logo.png` / `logo-white.png` / `favicon.ico` / `icon-192.png` / `apple-touch-icon.png`）を参照する。原本は `C:\Users\user\ロゴ素材\`（生成スクリプト `make_assets.py` 付き）。
- このLPはライト/ダークの両テーマ対応。**ヘッダーのロゴは黒文字版と白文字版の2枚を置き、CSSで出し分けている**（片方だけにすると暗い配色で社名が読めなくなる）。

## ターゲットと訴求（松岡さんの指示で確定）

**SIer・SES・受託のエンジニア**。読み手は求職者（採用企業向けではない）。

- FDE = Forward Deployed Engineer。顧客の現場に入り、自社プロダクトを顧客の業務に実装し、**使われるまで見届ける**エンジニア。
- ヒーロー「顧客の現場で、／**自分で書ける人**が足りない。」
- 論法は「同じことをやって、値段のつき方が変わる」。**成果物が納品物→顧客の業務が変わったこと／報酬の出どころが人月→プロダクトの売上**、の2点だけが違う、という置き方。
- 核のセクションは 03「なぜ、あなたが近いのか」＝**「あなたが"調整"と呼んできたものは、FDEの中核スキル」**。現場耐性・要件を引き出す力・エンタープライズの作法（稟議/情シス/セキュリティ）・納期・レガシーとデータの現実・業務ドメインの6資産。
- **「ただし自分でコードを書けること」を必ず条件として書く。**個人の努力不足に帰す書き方はしないが、ここだけは曖昧にしない。
- 実務的に効くのが 05「求人の探し方」＝**求人票に「FDE」とはほぼ書かれていない**（ソリューションエンジニア／インプリメンテーションエンジニア／導入エンジニア／テクニカルコンサルタント／カスタマーエンジニア／プロフェッショナルサービス等）。逆に同じ名前でも中身が違うので **①自分で書くのか ②課題設定に踏み込めるのか ③導入後まで持つのか** の3点で見る、という整理。
- 会社の型6枚: 外資AI・データ基盤／エンタープライズSaaS／公共・規制産業SaaS／生成AI業務実装／産業バーティカルSaaS／自社プロダクトを持つ開発会社。
- 06「向き不向き」で手放すもの（深く極める時間・安定稼働・リモート完結・作りたいものを作る自由・書かずに通す道）を明示。**「FDEはやめたほうがいい」と伝えることもある**と書いている。
- 07「選考までに埋められる差」＝ SQL／Python or TypeScript で1つ作りきる／API・クラウド／LLM APIの試作。目安3か月。

## ⚠ 統計値を一切載せていない（意図的な判断）

FDEは新しい職種で、市場規模・人数・年収の一次ソースが取れないため。ヒーローの数値ストリップ（他LPの `.hero-trust`）は使わず、**FDEの定義3点（Deploy / Build / Outcome）の `.hdef` に差し替え**ている。棒グラフ（`.hchart` `.bars`）も無し。
フッターの注記に「**FDEは法令や公的機関が定義した職種名ではなく、役割の範囲は企業ごとに異なる／当社が求人・面談で把握している一般的な傾向**」と明記済み。**次に数字を足したくなったら、必ず一次ソースを取ってから。**

## デザイン

マゼンタ（#C2185B／dark #F472B6）× シアン（#0E7490／dark #22D3EE）。他8本のどれとも被らない配色（既存: オレンジ/金/臙脂/青/紫/赤/緑/深緑）。ヒーロー背景は「現場へ降りていく」斜線グリッド（`.hero-deploy`）。

### ⚠ ナビCTAのコントラスト（この回で直した）

`.nav-links a{color:var(--ink-soft)}` の詳細度が `.btn-primary` に勝つため、**ヘッダーのCTAボタンの文字色が薄いピンクになる**。`.nav-links a.btn-primary{color:var(--on-accent)}` を足して解決。あわせて `--on-accent`（light #FFFFFF / dark #2B0714）を導入し、`.btn-primary` の `color:#fff` 直書きをやめた。
**この不具合は元テンプレート（embedded-lp 経由で pm-lp 由来）にあるので、既存8本にも同じ問題が残っている可能性が高い。**

---

## 求職者向けLP 共通ルール

- **CTA**（「話を聞いてみる」「無料で相談する」「面談予約」）のリンクは `https://calendly.com/r_matsuoka` 固定。`target="_blank" rel="noopener"` で開く。
- **GA4 測定ID** `G-1XXMP8Y1B4`（全サブドメイン共通プロパティ）。
- **ダークモード対応**（`prefers-color-scheme` ＋ `[data-theme]` の両対応）を壊さない。
- 事例・行き先は**社名・ロゴを出さない匿名アーキタイプ**で書く。
- 掲載する数字は**必ず一次ソース付き**。出典が取れない数字は載せず、空欄のままにする。架空の利用者の声や根拠のない自社指標（「年収アップ率○%」「独占求人多数」等）は入れない。
- 棒グラフの `.track` / `.fill` を `<span>` で書くと**描画されない**（inline要素なので width/height が効かない）。`display:block` が必要。
- ローカル確認は `file://` でも見えるが、簡易HTTPサーバー（node）を立てて `http://localhost:<port>/` で見るのが確実。ヘッドレスChromeのスクリーンショットも有効（`--screenshot` は絶対パス）。

## 相互リンク（求職者向け）

| サブドメイン | 内容 | リポジトリ |
|---|---|---|
| pm | PM特化 転職 | agentbest/pm-lp |
| eng | エンジニア→コンサル転職 | agentbest/engineer-consultant |
| ma | 未経験からM&A | agentbest/ma-lp |
| consul | 未経験からコンサル | agentbest/consultant-lp |
| engineer | 未経験からエンジニア | agentbest/engineer-lp |
| embedded | IoT・組込みエンジニア | agentbest/embedded-lp |
| consultingcareerchange50 | ファーム出身者の次のキャリア（50代） | agentbest/consultingcareerchange50 |
| student | 学生キャリア支援 | agentbest/student-career |
| **fde** | **FDE（本サイト）** | **agentbest/fde-lp** |

2026-08-29 に**双方向とも設置済み**：このページのフッター（→ 既存8本）、既存8本のフッター（→ fde）、コーポレート `agentbest/agentbest-lp` の `src/components/FooterSites.astro` の `specialSites['求職者の方へ']`。次に新しいLPを足すときは、この9本すべてを更新する。

⚠ 一括置換の罠: アンカーに `ft-sites-links` の文字列だけを使うと `<style>` 内のCSS定義にマッチして**ヘッダーnavに誤挿入される**。`<nav class="ft-links ft-sites-links">` のようにタグごと指定すること。

## push のルール

- ローカルで動作確認を済ませた変更は、**確認を取らずに commit & push してよい**。コミットメッセージは日本語。**push後は必ず何を変えたか報告する**。
- **以下に触れるときは必ず止まって事前確認する**:
  1. ドメイン・DNS・CNAME（DNSは**Squarespace Domains**管理・松岡さんの手作業）
  2. 個人情報・フォーム・認証
  3. 費用が発生する変更
  4. 既存ページ・データの削除
  5. 複数リポジトリへの一括変更（相互リンク更新など）
- Publicリポジトリ想定。push前にトークン・APIキーの混入をgrepで確認する。
- `main` への push = **即本番公開**。
